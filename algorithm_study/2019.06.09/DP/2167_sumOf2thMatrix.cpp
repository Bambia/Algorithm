#include <iostream>
using namespace std;

const int MAX_SIZE = 301;
int N,M;
int matrix[MAX_SIZE][MAX_SIZE];
int dp[MAX_SIZE][MAX_SIZE][MAX_SIZE][MAX_SIZE];

int main(){
    cin >> N >> M;
    for(int i=0; i<N; i++) {
        for(int j =0; j<M; j++){
            cin >> matrix[i][j];
        }
    }
    cin >> tc;
    while(tc--){
        
    }


    return 0;
}